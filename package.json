{
  "name": "umezoo-website",
  "version": "1.0.0",
  "description": "UMEZOO Inc. official website",
  "scripts": {
    "sync-note": "node scripts/sync-note-rss.js",
    "optimize-images": "node scripts/optimize-images.js",
    "build-partials": "node scripts/build-partials.js",
    "inject-img-dims": "node scripts/inject-img-dims.js",
    "generate-career-pages": "node scripts/generate-career-pages.js",
    "generate-news-jsonld": "node scripts/generate-news-jsonld.js",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "build": "npm run build-partials && npm run inject-img-dims && npm run generate-career-pages && npm run generate-news-jsonld && npm run generate-sitemap"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
